# Left-pad an integer sequence with zeros

Left-pad an integer sequence with zeros

## Usage

``` r
left_padded_sequence(x)
```

## Arguments

- x:

  A vector of integers.

## Value

A character vector of the same length as `x`, left-padded with zeros.
